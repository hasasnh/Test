# Test

Minimal ASP.NET Core 8 web project that responds with "Hello World!" at the root path.
Built with the .NET 8 SDK.

## Running

```bash
dotnet run
```

The server listens on `http://localhost:5000` by default (or `https://localhost:5001` for HTTPS).
